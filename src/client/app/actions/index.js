import { makeScreenActive, refreshScreen, registerKeyDown, registerKeyUp, playerAttack, createObstacle, setNewEnemyPos, changeEnemyDir } from './game.js';

export {
	makeScreenActive,
	refreshScreen,
	registerKeyDown,
	registerKeyUp,
	playerAttack,
	createObstacle,
	setNewEnemyPos,
	changeEnemyDir
}